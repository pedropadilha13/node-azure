<!DOCTYPE html>
<html lang="pt-br; en" id="home">
    <head>
        <title>Index</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=yes">
        <meta name="msapplication-TileColor" content="#ffffff">
        <meta name="msapplication-TileImage" content="/ms-icon-144x144.png">
        <meta name="theme-color" content="#ffffff">
        <!-- jQuery -->
        <script src="assets/js/jquery-3.3.1.min.js"></script>
        <script src="assets/js/jquery-ui.js"></script>
        <!-- Popper -->
        <script src="assets/js/popper.min.js"></script>
        <!-- Bootstrap -->
        <link rel="stylesheet" href="assets/css/bootstrap.min.css">
        <script src="assets/js/bootstrap.js"></script>
        <!-- Font Awesome -->
        <link rel="stylesheet" href="assets/css/font-awesome.css">
        <script src="assets/js/7df7e1332b.js"></script>
        <!-- FONTS  -->
        <link href="https://fonts.googleapis.com/css?family=PT+Sans" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css?family=Anton" rel="stylesheet">
        <!-- CSS -->
        <link rel="stylesheet" href="assets/css/jquery-ui.css">
        <link rel="stylesheet" href="assets/css/style.css">
        <!-- Google Chart Library -->
        <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    </head>
    <body id="body">

        <nav id="navbar" class="navbar navbar-expand-sm fixed-top bg-light navbar-top">
            <button class="navbar-toggler" type="button" id="navbar-toggler" data-toggle="collapse" data-target="#myNavbar" aria-controls="myNavbar" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon toggler"><i class="fa fa-bars"></i></span>
            </button>
            <a class="navbar-brand">Olá, <%= session.user.nome %> </a>
            <div class="collapse navbar-collapse navbar-to" id="myNavbar">
                <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                    <li class="nav-item active">
                        <button class="btn btn-warning nav-button" type="button" id="btnToggleGraph">Mostrar Gráfico</button>
                    </li>
                </ul>
                <ul class="navbar-nav mt-2 mt-lg-0">
                    <li class="nav-item active">
                        <button class="btn btn-danger" type="button" onclick="logout();">Logout</button>
                    </li>
                </ul>
            </div>
        </nav>

        <div class="row main-div" id="main-div">

            <div class="col-md-12 mx-auto">
                <div class="info col-md-12">Tela logada linda maravilhosa!</div>
                <div class="info col-md-12">Informações do cookie:</div>
                <br><br>
                <div class="info col-md-12">Olá <b>
                        <%= session.user.nome %></b>!</div>
                <div class="info col-md-12">Seu nome de usuário é <b>
                        <%= session.user.username %></b> e seu id é <b>
                        <%= session.user.id %></b>!</div>
            </div>
            <div class="col-md-12">
                <div id="chart_div" style="display: none;"></div>
            </div>
        </div>
        <script type="text/javascript" src="assets/js/main.js"></script>
        <% if (Array.isArray(scripts)) {
                scripts.forEach(script => { %>
                    <script type="text/javascript" src="assets/js/<%= script %>.js"></script>
             <% });
           } %>
    </body>
</html>
